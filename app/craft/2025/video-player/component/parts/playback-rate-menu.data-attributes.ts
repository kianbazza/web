export enum PlaybackRateMenuDataAttributes {
  /** The current playback rate value. */
  rate = 'data-rate',
}

export enum PlaybackRateMenuItemDataAttributes {
  /** Applied when this menu item is the active selection. */
  active = 'data-active',
}
