export enum PictureInPictureButtonDataAttributes {
  /** Applied when in picture-in-picture mode. */
  pip = 'data-pip',
  /** Applied when picture-in-picture is supported. */
  supported = 'data-supported',
}
