export enum QualityMenuDataAttributes {
  /** The current quality label. */
  quality = 'data-quality',
}

export enum QualityMenuItemDataAttributes {
  /** Applied when this menu item is the active selection. */
  active = 'data-active',
}
