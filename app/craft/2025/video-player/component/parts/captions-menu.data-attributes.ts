export enum CaptionsMenuDataAttributes {
  /** Applied when a caption track is active. */
  active = 'data-active',
}

export enum CaptionsMenuItemDataAttributes {
  /** Applied when this menu item is the active selection. */
  active = 'data-active',
}
